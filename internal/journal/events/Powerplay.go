package events

// Powerplay event structure
type PowerplayT interface{}

// Powerplay event handler
func (evHandler EventHandler) Powerplay(eventData map[string]interface{}) {
    // ev := new(PowerplayT)
    // mapstructure.Decode(eventData, ev)
}

