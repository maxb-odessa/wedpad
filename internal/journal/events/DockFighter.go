package events

// DockFighter event structure
type DockFighterT interface{}

// DockFighter event handler
func (evHandler EventHandler) DockFighter(eventData map[string]interface{}) {
    // ev := new(DockFighterT)
    // mapstructure.Decode(eventData, ev)
}

