package events

// RepairAll event structure
type RepairAllT interface{}

// RepairAll event handler
func (evHandler EventHandler) RepairAll(eventData map[string]interface{}) {
    // ev := new(RepairAllT)
    // mapstructure.Decode(eventData, ev)
}

